<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.function.template.mapper.BoardMapper">
    <select id="selectBoardList" resultType="com.function.template.dto.BoardDto">
            SELECT
                board_idx,
                title,
                hit_cnt,
                created_datetime
            FROM
                t_board
            WHERE
                deleted_yn = 'N'
            ORDER BY board_idx DESC
    </select>

    <insert id="insertBoard" parameterType="board.board.dto.BoardDto" useGeneratedKeys="true" keyProperty="boardIdx">
            INSERT INTO t_board
            (
                title,
                contents,
                created_datetime,
                creator_id
            )
            VALUE
            (
                #{title},
                #{contents},
                NOW(),
                'admin'
            )
    </insert>
    <update id="updateHitCount" parameterType="int">
        UPDATE
            t_board
        SET
            hit_cnt = hit_cnt + 1
        WHERE
            board_idx = #{boardIdx}
	</update>
    <select id="selectBoardDetail" parameterType="int" resultType="com.function.template.dto.BoardDto">
        SELECT
            board_idx, title, contents, hit_cnt,
            created_datetime,
            creator_id
        FROM
            t_board
        WHERE
            board_idx = ${boardIdx} AND deleted_yn = 'N'
	</select>
    <update id="updateBoard" parameterType="com.function.template.dto.BoardDto">
            t_board
        SET
            title = #{title},
            contents = #{contents},
            updated_datetime = NOW(),
            updater_id = 'admin'
        WHERE
            board_idx = #{boardIdx}
    </update>

    <update id="deleteBoard" parameterType="int">
        UPDATE
            t_board
        SET
            deleted_yn = 'Y',
            updated_datetime = NOW(),
            updater_id = 'admin'
        WHERE
            board_idx = #{boardIdx}
    </update>

    <insert id="insertBoardFileList" parameterType="com.function.template.dto.BoardFileDto">
        <![CDATA[
			INSERT INTO t_file
			(
				board_idx,
				original_file_name,
				stored_file_path,
				file_size,
				creator_id,
				created_datetime
			)
			VALUES
		]]>
        <foreach collection="list" item="item" separator=",">
            (
            #{item.boardIdx},
            #{item.originalFileName},
            #{item.storedFilePath},
            #{item.fileSize},
            'admin',
            NOW()
            )
        </foreach>
    </insert>

    <select id="selectBoardFileList" parameterType="int" resultType="com.function.template.dto.BoardFileDto">
		<![CDATA[
        SELECT
            idx,
            board_idx,
            original_file_name,
            FORMAT(ROUND(file_size / 1024), 0) AS file_size
        FROM
            t_file
        WHERE
            board_idx = #{boardIdx}
          AND deleted_yn = 'N'
        ]]>
	</select>
</mapper>